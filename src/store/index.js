import * as pages from './pages'
import * as cache from './cache'
import * as common from './common'

console.log({common, cache})

export const modules = {pages, cache, common}

// export const namespaced = true
